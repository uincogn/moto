
name: motouber
description: Sistema de controle financeiro para motoristas de aplicativo
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.3
  sqflite: ^2.3.3
  path: ^1.8.3
  intl: ^0.19.0
  fl_chart: ^0.68.0
  file_picker: ^8.0.0
  permission_handler: ^11.3.1
  path_provider: ^2.1.3
  share_plus: ^9.0.0
  # Temporariamente removido para build de teste
  # pdf: ^3.10.8
  # printing: ^5.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
